#include <iostream>
#include "Task0101.h"
#include "Task0102.h"
#include "Task0103.h"

using namespace std;

int main()
{
    srand(time(NULL));

    // // task-0101
    // vector<int> v1(10);
    // vector<int> v2(10);

    // Task0101 task = Task0101();
    // task.initVector(v1, 0, 5);
    // task.initVector(v2, 0, 5);

    // task.sumSims(v1, v2);

    // task.printVector(v1);
    // task.printVector(v2);

    // // task-0102
    // vector<int> v(10);

    // Task0102 task = Task0102();
    // task.initVector(v, -5, 10);

    // task.printVector(v);
    // v = task.findNegNums(v);
    // task.printVector(v);

    // task-010301
    // Task0103 task = Task0103();
    // cout << "Test01:\n";
    // task.formatLineVer01();

    // cout << "Test02:\n";
    // task.formatLineVer02();
}