#include <iostream>
#include "task-01.h"
#include "task-02.h"
#include "task-03.h"
#include "task-04.h"

#include <string>

#include <vector>
#include <algorithm>
#include <iomanip>

using namespace std;

int main()
{

    // // task-01
    // string cur_time_val;
    // Time prev_time = Time("00:00");

    // while (getline(cin, cur_time_val))
    // {
    //     Time cur_time = Time(cur_time_val);
    //     prev_time.addTime(cur_time);
    //     prev_time.printTime();
    // }

    // task-02
    // vector<int> v1 = {1, 3}; //{1, 2};
    // vector<int> v2 = {2};    //{3, 4};

    // Task0302 task = Task0302();
    // task.calc(v1, v2);

    // task-03
    // vector<char> inp = {'h', 'e', 'l', 'l', 'o'};
    // Task0303 task = Task0303();
    // task.calc(inp);

    // task-04
    // string target = "1234";                      // "7777";
    // vector<string> v = {"123", "4", "12", "34"}; // {"777", "7", "77", "77"};

    // Task0304 task = Task0304();
    // task.calc(v, target);
}